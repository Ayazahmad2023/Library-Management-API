# Library-Management-API
Backend REST API for a library system — FastAPI, SQLAlchemy ORM, JWT authentication, and SQLite — supporting book inventory, user roles, and borrow/return tracking across 21 endpoints.
